- name: Install dependencies
  run: |
    python -m pip install --upgrade pip
    {{#if dependency_file}}
    pip install -r "{{dependency_file}}"
    {{else}}
    {{#if pyproject}}
    pip install .[test]
    {{else}}
    echo "No dependency file provided"
    {{/if}}
    {{/if}}
